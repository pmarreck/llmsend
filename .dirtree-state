# Dirtree: Stateful directory trees in the CLI for humans and LLMs. https://github.com/pmarreck/dirtree
ver=1.2
annotate=[
	INTENT.md = Durable messaging purpose and agent-neutral Herdr delivery boundaries
	PLAN.md = Current llmsend protocol work and curiosity checks
	PROJECT_OVERVIEW.md = Defines LLMsend's durable two-channel architecture and resize-independent safety invariant.
	RULES.md = Non-negotiable delivery and data-safety invariants.
	flake.nix = Packages the cross-platform hook scripts and exposes the complete Nix check.
	monitors/monitors.json = Claude plugin monitor declaration that wakes idle sessions when the project inbox changes.
	skills/llmsend/SKILL.md = Durable inbox protocol, application monitors and authorized guarded Herdr wakes
	skills/llmsend/scripts/block-prompt-injection-hook = Compatibility-named PreToolUse advisory: warns on LLMsend-shaped terminal wakes but permits explicitly authorized inspect-first delivery.
	skills/llmsend/scripts/herdr_wake.lua = Pure ANSI composer classifier and injected-clock guarded wake state machine
	skills/llmsend/scripts/herdr_wake_cli.lua = Herdr wake adapter: identity checks, OS lock, persistent attempt journal
	skills/llmsend/scripts/inbox-awareness-hook = Agent-owned hook that reports changed pending inbox paths without touching the editor buffer.
	skills/llmsend/scripts/inbox-monitor = Session-scoped bounded inbox watcher that emits deduplicated Claude notifications without terminal input.
	skills/llmsend/scripts/notify-session = Herdr human notice by default; explicit --wake uses ANSI-aware draft protection
	skills/llmsend/scripts/write-note = Atomically writes collision-safe llmsend/v1 JSON-frontmatter notes from stdin.
	test = Runs the complete deterministic unit and integration suite.
	tests/integration/frontmatter_contract_test = Enforces the JSON-frontmatter sender, documentation, migration, and no-YAML contract.
	tests/integration/herdr_wake_cli_test = CLI transport, durable dedup and kernel lock checks
	tests/unit/herdr_wake_test.lua = Deterministic ANSI, draft, geometry, timing and retry regressions
	tests/unit/inbox_monitor_test = Deterministic monitor classifier, change-detection, deduplication, and resize-invariance tests.
	tests/unit/write_note_test = Covers JSON schema output, atomic collision suffixes, validation, and help behavior.
]
