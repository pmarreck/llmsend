# Dirtree: Stateful directory trees in the CLI for humans and LLMs. https://github.com/pmarreck/dirtree
ver=1.2
annotate=[
	PLAN.md = Current llmsend protocol work and curiosity checks
	PROJECT_OVERVIEW.md = Defines LLMsend's durable two-channel architecture and resize-independent safety invariant.
	RULES.md = Non-negotiable delivery and data-safety invariants.
	flake.nix = Packages the cross-platform hook scripts and exposes the complete Nix check.
	monitors/monitors.json = Claude plugin monitor declaration that wakes idle sessions when the project inbox changes.
	skills/llmsend/SKILL.md = Durable inbox plus backend-specific local and tailnet tmux notification protocol for Claude and Codex
	skills/llmsend/scripts/block-prompt-injection-hook = Migration guard that blocks LLMsend-shaped terminal input injection.
	skills/llmsend/scripts/inbox-awareness-hook = Agent-owned hook that reports changed pending inbox paths without touching the editor buffer.
	skills/llmsend/scripts/inbox-monitor = Session-scoped bounded inbox watcher that emits deduplicated Claude notifications without terminal input.
	skills/llmsend/scripts/notify-session = Human-only tmux status notification with no pane input mutation.
	test = Runs the complete deterministic unit and integration suite.
	tests/unit/inbox_monitor_test = Deterministic monitor classifier, change-detection, deduplication, and resize-invariance tests.
]
